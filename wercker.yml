box: 
  id: node:9
    
dev:
  steps:
    - wercker/npm-install@2.0.1
    - wercker/npm-test@1.0.1

# Build definition
build:
  # The steps that will be executed on build
  steps:
    # A step that executes `npm install` command
    - wercker/npm-install@2.0.1
    # A step that executes `npm test` command
    - wercker/npm-test@1.0.1
    - maxon/npm-run@0.1.2:
          script: build
